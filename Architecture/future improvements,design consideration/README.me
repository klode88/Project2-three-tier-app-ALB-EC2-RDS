✅ Services that could be added

-AWS WAF

Protects the Application Load Balancer from SQL injection, XSS, bots, and abusive traffic.

Adds an extra security layer at the edge of the architecture.

-Auto Scaling Group (ASG)

Automatically replaces unhealthy EC2 instances.

Scales the application tier up and down based on traffic.

-RDS Multi-AZ

Provides automatic failover if the primary database goes down.

Increases availability and reliability.

-CloudFront CDN

Caches content closer to users.
HTTPS (ACM + ALB)

Encrypts traffic from users to the application.

Required for real production systems.

-Secrets Manager / SSM Parameter Store

Secure storage for database credentials and application secrets.

Prevents hard-coding sensitive data.

Why these services were not enabled in this project;

To avoid unnecessary AWS costs while learning and building the core architecture.

The architecture was designed so these services can be added easily later without redesigning the network.

cost awareness, which is important in real cloud environments.
